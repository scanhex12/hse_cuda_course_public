.base: &base
  image: nvidia/cuda:12.2.0-devel-ubuntu22.04
  before_script:
    - ls -la /dev/nvidia* || echo "No GPU devices"
    - apt-get update -qq && apt-get install -y -qq cmake git clang-format

00-hello-world:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/00-hello-world"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_hello_world
    - ./test_hello_world

01-fibonacci:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/01-fibonacci"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_fibonacci
    - ./test_fibonacci

02-binary-image:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/02-binary-image"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_binary_image
    - ./test_binary_image
    - cmake --build . -j16 --target benchmark_binary_image
    - ./benchmark_binary_image

03-tree:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/03-tree"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_tree
    - ./test_tree
    - cmake --build . -j16 --target benchmark_tree
    - ./benchmark_tree

04-vector:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/04-vector"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_vector
    - ./test_vector
    - cmake --build . -j16 --target benchmark_vector
    - ./benchmark_vector

05-transpose:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/05-transpose"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_transpose
    - ./test_transpose
    - cmake --build . -j16 --target benchmark_transpose
    - ./benchmark_transpose

06-gemm-v0:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/06-gemm-v0"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_gemm_v0
    - ./test_gemm_v0
    - cmake --build . -j16 --target benchmark_gemm_v0
    - ./benchmark_gemm_v0

07-prefsums:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/07-prefsums"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_prefsums
    - ./test_prefsums
    - cmake --build . -j16 --target benchmark_prefsums
    - ./benchmark_prefsums

08-sort:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/08-sort"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_sort
    - ./test_sort
    - cmake --build . -j16 --target benchmark_sort
    - ./benchmark_sort

09-reduce-norm:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/09-reduce-norm"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_reduce_norm
    - ./test_reduce_norm
    - cmake --build . -j16 --target benchmark_reduce_norm
    - ./benchmark_reduce_norm

10-moe-selection:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/10-moe-selection"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_moe_selection
    - ./test_moe_selection
    - cmake --build . -j16 --target benchmark_moe_selection
    - ./benchmark_moe_selection

11-reduce-flash-attention:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/11-reduce-flash-attention"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_reduce_flash_attention
    - ./test_reduce_flash_attention
    - cmake --build . -j16 --target benchmark_reduce_flash_attention
    - ./benchmark_reduce_flash_attention

12-tensorcores:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/12-tensorcores"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_lu_decomposition
    - ./test_lu_decomposition
    - cmake --build . -j16 --target benchmark_lu_decomposition
    - ./benchmark_lu_decomposition

13-streaming-vad:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/13-streaming-vad"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_streaming_vad
    - ./test_streaming_vad
    - cmake --build . -j16 --target benchmark_streaming_vad
    - ./benchmark_streaming_vad

14-db:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/14-db"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . -j16 --target test_db
    - ./test_db
    - cmake --build . -j16 --target benchmark_db
    - ./benchmark_db
