.base: &base
  image: nvidia/cuda:12.2.0-devel-ubuntu22.04
  before_script:
    - ls -la /dev/nvidia* || echo "No GPU devices"
    - apt-get update -qq && apt-get install -y -qq cmake git clang-format

00-hello-world:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/00-hello-world"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . --target test_hello_world
    - ./test_hello_world

01-fibonacci:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/01-fibonacci"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . --target test_fibonacci
    - ./test_fibonacci

02-binary-image:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/02-binary-image"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . --target test_binary_image
    - ./test_binary_image
    - cmake --build . --target benchmark_binary_image
    - ./benchmark_binary_image

03-tree:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/03-tree"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . --target test_tree
    - ./test_tree
    - cmake --build . --target benchmark_tree
    - ./benchmark_tree

04-vector:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/04-vector"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . --target test_vector
    - ./test_vector
    - cmake --build . --target benchmark_vector
    - ./benchmark_vector

05-transpose:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/05-transpose"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . --target test_transpose
    - ./test_transpose
    - cmake --build . --target benchmark_transpose
    - ./benchmark_transpose

06-gemm-v0:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/06-gemm-v0"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . --target test_gemm_v0
    - ./test_gemm_v0
    - cmake --build . --target benchmark_gemm_v0
    - ./benchmark_gemm_v0

07-prefsums:
  <<: *base
  rules:
    - if: $CI_COMMIT_BRANCH == "submits/07-prefsums"
  script:
    - mkdir build && cd build
    - cmake .. -DCMAKE_BUILD_TYPE=Release
    - cmake --build . --target test_prefsums
    - ./test_prefsums
    - cmake --build . --target benchmark_prefsums
    - ./benchmark_prefsums
