# Triton Image — RGB → оттенки серого

## Задача

Дан цветной тензор изображения `img` формы `(H, W, 3)` (каналы R, G, B). Нужно получить изображение того же размера `(H, W, 3)`, в котором каждый пиксель заменён на оттенок серого с той же яркостью:

```
gray = 0.2 * R + 0.5 * G + 0.3 * B
```
